node_modules/
dist/
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# dotnet build scratch folders for smtc-helper -- always regenerated by
# `dotnet publish`, never need to be committed.
smtc-helper/bin/
smtc-helper/obj/
# NOTE: smtc-helper/publish/ (the actual compiled smtc-helper.exe) is
# intentionally NOT ignored -- it's committed directly so anyone who clones
# the repo has a working exe without needing the .NET SDK installed.
